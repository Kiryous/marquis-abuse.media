.container
  h1.name 🎩 Маркиз для <a href="http://abuse.media">abuse.media</a>
  p 1. Выберите фоновую фотографию
  // Загрузка картинки
  input#files(type='file', name='files[]', multiple='')
  |  или
  input#imageUrl(type='text', placeholder='Cсылка на картинку', onchange='changeBg(this.value)')
  p 2. Наберите заголовок новости и отрегулируйте положение фона
  // Скрытые поля определяющие шрифт и размер шрифта
  form#title-font
    | font-family:
    input#title-font-family(type='text', value='Permian Sans')
    | font-size:
    input#title-font-size(type='number', value='52')
    | px  
    | font-weight:
    input#title-font-weight(type='text', value='600')
  // WISWIG окошко
  .cover-container
    .cover#cover
      #text
        textarea#title(placeholder='Заголовок новости')
        // (На будущее, для материала) <input id="subtitle" type="text" placeholder="Подзаголовок"/>
      span.logo abuse.media
    // Регулятор положения фона
    input#bg-position(type='range', min='0', max='100', step='0.1', orient='vertical', oninput='changeBgPosition(this.value)')
  p 4. Посмотрите, как картинка будет выглядеть в сетях и опенграфе
  // Предпросмотр постов в соцсетях
  //
    <div id="networks-preview">
    <canvas id="fb-post"></canvas>
    <canvas id="vk-post"></canvas>
    <canvas id="twi-post"></canvas>
    </div>
  // Предпросмотр опенграфа
  //
    <div id="opengraph-preview">
    <canvas id="fb-post"></canvas>
    <canvas id="vk-post"></canvas>
    <canvas id="twi-post"></canvas>
    </div>
  #result
    // Скачивалка изображения и дебагер
    .result__buttons
      a.result__button#download-button(onclick="downloadImg(this,'image-debug')") Скачать картинку
      a.result__button.button(onclick='showPreview(this)') Показать
    // Канвас-дебагер
    canvas#image-debug(width='960px', height='480')
      | Ваш браузер не поддерживает <canvas>. Воспользуйтесь Гугль-Хромом или другим современным браузером
img.test-image#background-image(src='http://yandex.ru/images/today?size=1920x1200')
